## Zippee

A simple zip command utility writing nodejs.

### Install

```shell
npm i -g zippee
```

### Config
You can write config in

[
  'package.json',
  `.syncenvrc`,
  `.syncenvrc.json`,
  `.syncenvrc.yaml`,
  `.syncenvrc.yml`,
  `.syncenvrc.js`,
  `syncenv.config.js`,
]

### Usage
